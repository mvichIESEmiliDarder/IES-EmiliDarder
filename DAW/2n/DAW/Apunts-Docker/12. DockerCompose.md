Aquesta és una presentació sobre **Docker Compose** amb la informació extreta de l'enllaç proporcionat.

-----

## 💻 Presentació: Què és Docker Compose?

### Diapositiva 1: Introducció i Definició

**Títol:** Docker Compose: L'Orquestració d'Aplicacions Multi-Contenidor

  * **Què és?**

      * És una eina per a **definir i gestionar aplicacions multi-contenidor** de forma senzilla.
      * Permet configurar un entorn complet (amb diversos serveis, xarxes i volums) utilitzant un sol fitxer de configuració.

  * **Objectiu:**

      * Simplificar la gestió d'entorns de desenvolupament que requereixen múltiples serveis interconnectats (per exemple, un servidor web, una base de dades i una memòria cau).

  * **Funcionament:**

      * Tot l'entorn es descriu en un fitxer anomenat **`docker-compose.yml`**.
      * Amb una sola comanda (`docker-compose up`), pots crear i executar tots els serveis definits.

-----

### Diapositiva 2: Docker vs. Docker Compose vs. Kubernetes

| Eina | Enfocament | Ús Ideal |
| :--- | :--- | :--- |
| **Docker** | Plataforma de contenidors. | Creació i execució de **contenidors individuals** aïllats. |
| **Docker Compose** | Eina de complement i orquestració. | **Coordinació de múltiples contenidors** per a un entorn de desenvolupament complet. Configuració ràpida i senzilla. |
| **Kubernetes** | Plataforma d'orquestració robusta. | Desplegament i gestió d'aplicacions a **producció a gran escala**. Ofereix alta disponibilitat i escalabilitat. |

  * Docker Compose és perfecte per a **entorns de desenvolupament i proves** locals.

-----

### Diapositiva 3: El Cor: El Fitxer `docker-compose.yml`

El fitxer `docker-compose.yml` és el manifest de la teva aplicació. Està escrit en format **YAML** i segueix una estructura bàsica:

**Estructura Típica:**

```yaml
version: '3.8' 

services:
  # Aquí es defineix cada contenidor (servei)
  web:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./app:/usr/share/nginx/html
    depends_on:
      - database

  database:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=mydb

networks:
  # Definició de xarxes personalitzades (opcional)
  app_network:
    driver: bridge

volumes:
  # Definició de volums per a persistència de dades
  db_data:
```

-----

### Diapositiva 4: Conceptes Clau

#### Serveis (`services`)

Són els contenidors de la teva aplicació. Es defineixen amb paràmetres com:

  * `image`: La imatge de Docker a utilitzar (e.g., `mysql`, `nginx`).
  * `ports`: El mapeig de ports entre el host i el contenidor.
  * `volumes`: Permet muntar dades o directoris del host dins del contenidor.
  * `environment`: Variables d'entorn necessàries per al contenidor.
  * `depends_on`: Estableix que un servei no s'iniciarà fins que els serveis dels quals depèn estiguin en funcionament (e.g., la web depèn de la base de dades).

#### Volums (`volumes`)

  * S'utilitzen per a la **persistència de dades**. Asseguren que les dades no es perdin en aturar o reiniciar els contenidors.
  * Poden ser volums gestionats per Docker o directoris compartits amb el host.

#### Xarxes (`networks`)

  * Permeten crear **xarxes personalitzades** i aïllades per a controlar la comunicació entre els serveis de l'aplicació.

-----

### Diapositiva 5: Comandes Essencials de Gestió

Un cop tens el fitxer `docker-compose.yml`, pots gestionar l'entorn amb facilitat:

| Comanda | Funció |
| :--- | :--- |
| `docker-compose up` | Construeix (si cal) i **inicia tots els serveis** definits en primer pla. |
| `docker-compose up -d` | Construeix i inicia tots els serveis en **mode deslligat** (*detached*), deixant la terminal lliure. |
| `docker-compose stop` | Atura els contenidors en execució. |
| `docker-compose start` | Inicia els contenidors aturats. |
| `docker-compose restart` | Reinicia els serveis. |
| `docker-compose ps` | Mostra l'estat actual de tots els contenidors de l'entorn. |
| `docker-compose logs` | Mostra els registres (*logs*) de tots els serveis. |
| `docker-compose down` | Atura i **elimina** els contenidors, xarxes i volums (si no es defineix el contrari). |
| `docker-compose up --scale web=3` | **Escala** el servei `web` per tenir 3 instàncies en execució. |

-----

### Diapositiva 6: Resum i Importància

**Per què utilitzar Docker Compose?**

1.  **Replicabilitat:** Assegura que tots els desenvolupadors (i els entorns de prova) tinguin una configuració de serveis **idèntica**.
2.  **Eficiència:** Simplifica el procés d'inici i aturada d'aplicacions complexes a una sola comanda.
3.  **Definició Centralitzada:** Tota la lògica de l'entorn es troba en un sol fitxer fàcil de versionar i compartir.

**En resum, Docker Compose és la millor eina per a configurar i orquestrar de manera eficient un entorn de desenvolupament o prova multi-contenidor.**